name: Recover v0.3.13 source temporarily

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  recover:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm workflow
        run: echo "Recovery workflow is running"
